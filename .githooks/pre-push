#!/usr/bin/env bash
set -euo pipefail

if ! command -v pre-commit >/dev/null 2>&1; then
  echo "pre-commit not installed. Install with: pipx install pre-commit" >&2
  exit 1
fi

SKIP=codex-autofix-fallback pre-commit run --hook-stage pre-push
